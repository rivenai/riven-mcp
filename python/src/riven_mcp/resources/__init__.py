"""Riven MCP Server — Resource definitions.

Each resource module exports a `register(server)` function that registers
its resource(s) on the MCPServer instance using the @server.resource() decorator.
"""
